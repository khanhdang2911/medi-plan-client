/** @format */

const images = {
	logo: require('./images/logo.svg').default,
	navbar: require('./images/navbar.svg').default,
	appointment: require('./images/lichhen.svg').default,
}
export default images
